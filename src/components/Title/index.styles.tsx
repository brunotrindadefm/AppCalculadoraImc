import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textTitle: {
        fontSize: 20,
        fontWeight: 500,
        marginBottom: 12
    }

})

export default styles